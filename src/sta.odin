package main

STAResults :: struct {}

run_sta :: proc(netlist_hypergraph: ^NetlistHyperGraph) {
	// TODO(rahul): convert the hgr to a dag for running sta (this is super quick when run non topologically), the slowness comes from topo analysis later

	// for vertex in netlist_hypergraph.cells {

	// }
	// for edge in netlist_hypergraph.nets {

	// }
}
