# Scratchpad folder contents

This folder contains data needed to run/test this rtl->gds flow.
Every folder will have a verilog design and the tcl scripts/resources needed to run our flow.
Currently there is only a gcd folder with a simple gcd.v.
On this we use a tcl script to run yosys and convert to netlist, so we can do PnR on this netlist.
